## Pablo v1.7.3

### Fixed

- Remote `docker compose` failures now show the remote command stdout/stderr instead of only exit status 1.

### Downloads

| Platform | File |
|----------|------|
| macOS (Intel) | pablo-darwin-amd64 |
| macOS (Apple Silicon) | pablo-darwin-arm64 |
| Linux (amd64) | pablo-linux-amd64 |
| Windows (amd64) | pablo-windows-amd64.exe |
| Windows (arm64) | pablo-windows-arm64.exe |
| VS Code extension | pablo-1.7.3.vsix |
| Visual Studio 2026 extension | pablo-vs2026-1.7.3.vsix |

Verify downloads with checksums.txt (SHA-256).
