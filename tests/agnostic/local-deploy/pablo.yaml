name: local-test-app
version: 0.0.1

profiles:
  local-test:
    type: static
    environments:
      dev:
        deploy:
          source:
            dir: ./src
            include: ["*.html"]
          target_path: ./dist
          strategy: overwrite
