name: win-rename-replace-test
version: 1.0.0

profiles:
  default:
    type: static
    environments:
      local:
        deploy:
          source:
            dir: ./src
            include: ["*.txt"]
          target_path: "C:\\PabloTest\\RenameReplace"
          strategy: rename-replace
