name: win-rename-replace-test
version: 1.0.0

profiles:
  default:
    type: static
    output_dir:
      dir: ./src
      include: ["*.txt"]
    environments:
      local:
        deploy:
          target_path: "C:\\PabloTest\\RenameReplace"
          strategy: rename-replace
